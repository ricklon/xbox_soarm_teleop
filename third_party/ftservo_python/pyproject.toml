[project]
name = "scservo_sdk"
version = "0.0.0"
description = "Vendored Feetech SCServo SDK (FTServo_Python)"
requires-python = ">=3.10,<3.11"
license = {file = "LICENSE"}

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["scservo_sdk"]
